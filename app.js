let photoCount = document.getElementById('photoCount');
let submitBtn = document.getElementById('excute');

function FsubmitBtn(value) {
  let str = '';
  let submitValue = photoCount.value;
  str = submitValue;
  console.lot(str);
}
submitBtn.addEventListener('click', FsubmitBtn);
